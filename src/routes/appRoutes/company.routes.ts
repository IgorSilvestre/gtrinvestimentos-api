import { Router } from "express";

export const companyRouter = Router()

companyRouter.post('/', () => (''))

companyRouter.get('/', () => (''))

companyRouter.put('/', () => (''))

companyRouter.delete('/', () => (''))